# Local development Redis configuration.
maxmemory 256mb
maxmemory-policy allkeys-lru

# Durability for queues (BullMQ) and cache survival across restarts.
appendonly yes
appendfsync everysec

save 900 1
save 300 10
